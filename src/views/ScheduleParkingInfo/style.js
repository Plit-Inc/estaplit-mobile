import styled from "styled-components";
import * as Constants from '../../constants';

export const ScheduleParkingInfoStyle = styled.View`
  justify-content: space-between;
  background-color: ${Constants.colors.gray[100]};
  height: 100%;
  padding: 20px;
`;